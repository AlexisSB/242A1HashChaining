#include <stdio.h>
#include <stdlib.h>

struct htablerec *htable{
  int num_items;
    };

void   htable_free(htable h);
int    htable_insert(htable h, char *str);
htable htable_new(int capacity);
void   htable_print(htable h, FILE *stream);
int    htable_search(htable h, char *str
